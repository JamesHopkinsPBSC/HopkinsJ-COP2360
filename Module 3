using System;

class Octopus
{
    string name;
    public int weight kg = 10;
    static readonly int legs = 8;
    static readonly int beak = 1;
    static readonly int brain = 9;
  

    public Octopus(string name)
    {
        Name = name;
    }
}

class Program
{
    static void Main()
    {

// Create a new Octopus object named "Inky"
Octopus myOctopus = new Octopus("Inky");
        Console.WriteLine("Age of Inky: " + o.Age);
        Console.WriteLine("Name Inky: " + o.Name);
        Console.ReadLine();
    }
}
